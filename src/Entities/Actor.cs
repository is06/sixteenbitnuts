using Microsoft.Xna.Framework;

namespace SixteenBitNuts
{
    public abstract class Actor : Entity
    {
        public Actor(Map map, string name) : base(map, name)
        {
            
        }
    }
}
